# Copy this file to .env and replace with your real values.
# Never commit .env with secrets (it should stay gitignored).

MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.example.mongodb.net/inventory-monitor?retryWrites=true&w=majority
PORT=3001
